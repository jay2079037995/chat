# v1.23.0 测试文档 — Bot 聊天修复 + Agent 文件产物发送 + 交互式 UI 重设计

## 自动化测试

### Server 测试
- [AUTO] `server-bot-generic-tools.test.ts` — 工具数量 5→6 + send_file_to_chat
- [AUTO] `bot.test.ts` — 流式聊天 ReadableStream 安全消费
- [AUTO] metadata 更新 handler（message:update-metadata socket 事件）

### Electron 测试
- [AUTO] `generic-tool-executor.test.ts` — read_file_binary 单文件 + 目录 zip

### Client 测试
- [AUTO] `InteractiveOptions.test.tsx` — 决策卡片渲染 + 富选项 + 点击 + 已选状态
- [AUTO] `MessageBubble.test.tsx` — metadata 扩展字段传递

## 真实 LLM 验证（DeepSeek）— `bot-llm-integration.test.ts`
- [AUTO] createModel + generateText 正常生成回复（真实 DeepSeek API 调用）
- [AUTO] createModel + streamText 正常流式输出（真实 DeepSeek API 调用）
- [AUTO] present_choices — single_select 生成 richItems metadata
- [AUTO] present_choices — 纯字符串选项不生成 richItems
- [AUTO] present_choices — text_input 生成 inputRequest metadata
- [AUTO] send_file_to_chat — dispatcher 获取文件 + artifact 回调
- [AUTO] send_file_to_chat — dispatcher 返回错误处理

## 构建验证
- [AUTO] `pnpm build` 全部编译成功
- [AUTO] `pnpm test` 全部通过
