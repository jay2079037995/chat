# v2.5.0 测试文档 — Bug 修复 + 体验优化 + Skill 安装修复 + 401 认证修复

## 自动化测试

### Server 测试

#### 对话管理
- [AUTO] `conversation-management.test.ts` — clearConversationMessages 清空消息（Redis 消息 Hash + 有序集合 + 未读计数全部删除）
- [AUTO] `conversation-management.test.ts` — clearConversationMessages 空会话处理（返回 0）
- [AUTO] `conversation-management.test.ts` — POST /conversations/:id/clear 端点（200 响应 + deletedCount）
- [AUTO] `conversation-management.test.ts` — 清空记录权限校验（非会话成员返回 403）

#### Agent 日志
- [AUTO] `server-tool-bridge.test.ts` — ToolLogData 包含 outputLength 字段
- [AUTO] `server-tool-bridge.test.ts` — 工具输出截断上限 5000 字符

#### Skill 安装
- [AUTO] `server-tool-bridge.test.ts` — install_skill 工具 schema 包含 name 参数

### Electron 测试

#### Skill 安装
- [AUTO] `generic-tool-executor.test.ts` — search_skills 缓存 PluginEntry[] 到 lastSearchResults
- [AUTO] `generic-tool-executor.test.ts` — install_skill 通过 name 参数从缓存查找 PluginEntry
- [AUTO] `generic-tool-executor.test.ts` — install_skill 未搜索直接用 name → 提示先用 search_skills

#### 工具日志
- [AUTO] `generic-tool-executor.test.ts` — 控制台详细日志输出（参数/workspace/耗时/输出预览）
- [AUTO] `generic-tool-executor.test.ts` — write_file 返回绝对路径

### Client 测试（组件测试）

#### Bot 聊天修复
- [AUTO] `ChatWindow.test.tsx`（已有） — Bot 对话 fakeMsg 合并 store 消息 metadata
- [AUTO] `ChatWindow.test.tsx`（已有） — Bot 流式回复时自动滚动到底部
- [AUTO] `ChatWindow.test.tsx`（已有） — Bot 对话渲染 type=file/image 的消息

#### 对话管理
- [AUTO] `ConversationContextMenu.test.tsx`（已有） — Popconfirm 确认点击不被 click-outside 中断
- [AUTO] `ConversationContextMenu.test.tsx`（已有） — 清空记录菜单项显示 + Popconfirm 确认

### 覆盖项

#### Agent 日志增强
- [AUTO] AgentStepLog 新增 toolOutputLength/workspacePath 字段 → TypeScript 编译验证
- [AUTO] 工具调用摘要显示 → `BotLogViewer.test.tsx`（已有）
- [AUTO] 输出大小标签显示 → `BotLogViewer.test.tsx`（已有）

#### Bot 聊天修复
- [AUTO] present_choices 交互选项渲染（metadata 合并）→ `ChatWindow.test.tsx`
- [AUTO] 流式回复自动滚动（300ms 间隔）→ `ChatWindow.test.tsx`
- [AUTO] send_file_to_chat 文件预览/下载 → `ChatWindow.test.tsx`

#### Bot 工作区可见性
- [AUTO] Bot 列表显示工作目录路径 → `BotManager.test.tsx`（已有）
- [AUTO] write_file 返回绝对路径 → `generic-tool-executor.test.ts`

#### 对话管理修复
- [AUTO] Popconfirm click-outside 排除 .ant-popconfirm/.ant-popover → `ConversationContextMenu.test.tsx`
- [AUTO] clearConversationMessages 全链路（Redis→Service→API→Client）→ `conversation-management.test.ts`
- [AUTO] 清空记录权限校验（403 Forbidden）→ `conversation-management.test.ts`

#### Skill 安装修复
- [AUTO] GitHub 整目录下载（downloadGitHubDir 递归）→ `generic-tool-executor.test.ts`
- [AUTO] Agent install_skill name 参数从缓存查找 → `generic-tool-executor.test.ts`
- [AUTO] Fallback 模式（url 参数仅下载 SKILL.md）→ `generic-tool-executor.test.ts`
- [AUTO] GitHub API 限速处理（403/429 友好提示）→ `generic-tool-executor.test.ts`

#### 401 认证修复
- [AUTO] 401 拦截器自动用 token 重建 session → `api.test.ts`（集成于 auth.test.ts）
- [AUTO] 重建成功自动重试原始请求 → `auth.test.ts`
- [AUTO] 重建失败跳转登录页 → `auth.test.ts`
- [AUTO] 并发请求共享 refreshingSession Promise → `auth.test.ts`
- [AUTO] _retried 标记防止无限循环 → `auth.test.ts`

## 构建验证
- [AUTO] `pnpm build` 全部编译成功
- [AUTO] `pnpm test` 全部通过
