# v1.26.0 测试文档 — 完整 LLM 调用日志 + 细粒度步骤进度

## 自动化测试

### Server 测试
- [AUTO] `bot-llm-log-api.test.ts` — llmLog 端点逻辑（saveLLMCallLog Redis 保存、botId 覆盖、错误日志、tools 日志）

### Agent-app 测试
- [AUTO] `botClient.test.ts` — saveLLMCallLog 方法（方法存在性 + 网络错误处理）
- [AUTO] `agentManager.test.ts` — Agent 管理器（generateLegacy mock 兼容）

### Client 测试
- [AUTO] `BotStepProgress.test.tsx` — loading_history 步骤标签渲染

### 覆盖项
- [AUTO] LLMCallLog 保存到 Redis → `bot-llm-log-api.test.ts` saveLLMCallLog 测试
- [AUTO] botId 强制覆盖（安全）→ `bot-llm-log-api.test.ts` 覆盖验证
- [AUTO] 错误 LLM 日志（无 response 有 error）→ `bot-llm-log-api.test.ts` 错误日志测试
- [AUTO] 包含 tools 的 LLM 日志 → `bot-llm-log-api.test.ts` tools 测试
- [AUTO] BotClient.saveLLMCallLog 方法可用 → `botClient.test.ts` 方法存在性 + 网络错误
- [AUTO] BotStepIndicator loading_history 标签 → `BotStepProgress.test.tsx`
- [AUTO] BotLogViewer "LLM 日志" Tab 显示数据 → 复用已有 `BotLogViewer.test.tsx` 覆盖
- [AUTO] ServerBotRunner 保存 LLM 日志 → 集成于 `bot-llm-log-api.test.ts` saveLLMCallLog

## 构建验证
- [AUTO] `pnpm build` 全部编译成功
- [AUTO] `pnpm test` 全部通过
