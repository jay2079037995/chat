# v0.3.0 - 一对一聊天 测试文档

> 标记说明: `[AUTO]` = 自动化测试覆盖 | `[MANUAL]` = 需手动验证

## 1. WebSocket 连接

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 1.1 | [AUTO] (E2E) | 携带有效 Session ID 连接 WebSocket | 连接成功，服务端日志显示 client connected | [x] |
| 1.2 | [MANUAL] | 携带无效 Session ID 连接 WebSocket | 连接被拒绝，返回认证错误 | [ ] |
| 1.3 | [MANUAL] | 不携带 Session ID 连接 WebSocket | 连接被拒绝 | [ ] |
| 1.4 | [MANUAL] | 连接后 Session 过期/被销毁 | 服务端主动断开连接 | [ ] |
| 1.5 | [MANUAL] | 网络断开后自动重连 | 客户端自动尝试重新连接 | [ ] |

## 2. 私聊消息收发

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 2.1 | [AUTO] (E2E) | 用户 A 向用户 B 发送文字消息 | 消息出现在聊天窗口 | [x] |
| 2.2 | [MANUAL] | 用户 B 回复用户 A | 用户 A 实时收到回复（需双浏览器验证） | [ ] |
| 2.3 | [AUTO] (Jest) | 消息包含完整信息 | 消息体包含 id, senderId, content, type, createdAt | [x] |
| 2.4 | [AUTO] (Jest) | 消息存储到 Redis | 创建会话后可通过 API 查询到 | [x] |
| 2.5 | [MANUAL] | 发送空消息 | 被拒绝，返回错误提示 | [ ] |

## 3. 会话列表

### 3.1 后端 API

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 3.1.1 | [AUTO] (Jest+E2E) | 获取当前用户的会话列表 | 返回会话列表，包含 participantNames | [x] |
| 3.1.2 | [AUTO] (RTL) | 会话包含最后一条消息摘要 | ConversationList 渲染 lastMessage 预览 | [x] |
| 3.1.3 | [MANUAL] | 新消息到达时会话排序更新 | 最新消息的会话排在最前面 | [ ] |

### 3.2 前端组件

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 3.2.1 | [AUTO] (RTL+E2E) | 会话列表正确渲染 | 显示对方用户名、最后消息、时间 | [x] |
| 3.2.2 | [AUTO] (RTL) | 点击会话切换聊天窗口 | 调用 selectConversation | [x] |
| 3.2.3 | [MANUAL] | 收到新消息时列表实时更新 | 对应会话的最后消息和排序更新 | [ ] |

### 3.3 接收方会话列表

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 3.3.1 | [AUTO] (E2E) | 用户 A 发消息后，用户 B 的会话列表出现该会话 | B 调用 GET /conversations 能看到与 A 的会话 | [x] |
| 3.3.2 | [AUTO] (E2E) | 接收方 Socket 能收到新会话的消息 | B 的 socket 被自动加入会话房间，收到 message:receive | [x] |

## 4. 聊天窗口

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 4.1 | [AUTO] (RTL) | 聊天窗口显示历史消息 | 渲染消息列表（你好呀、你也好） | [x] |
| 4.2 | [AUTO] (RTL+E2E) | 输入框输入文字并发送 | 消息出现在聊天区域，调用 sendMessage | [x] |
| 4.3 | [AUTO] (E2E) | 按 Enter 发送消息 | 消息发送成功 | [x] |
| 4.4 | [MANUAL] | 自己的消息靠右，对方消息靠左 | 消息气泡左右分布正确 | [ ] |
| 4.5 | [MANUAL] | 消息显示发送时间 | 每条消息下方或旁边显示时间 | [ ] |

## 5. 在线状态

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 5.1 | [AUTO] (E2E) | 用户 B 先在线，用户 A 后连接并搜索 B 打开聊天 | A 看到 B 显示"在线" | [x] |
| 5.2 | [AUTO] (E2E) | 用户 B 下线后 | A 看到 B 变为"离线" | [x] |
| 5.3 | [MANUAL] | 会话列表显示在线状态 | 在线用户有绿色指示 | [ ] |
| 5.4 | [AUTO] (RTL) | 聊天窗口显示对方在线状态 | 顶部显示"在线" | [x] |

## 6. 消息已读/未读

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 6.1 | [AUTO] (RTL) | 收到新消息未打开会话 | 会话列表显示未读消息数量 badge | [x] |
| 6.2 | [AUTO] (Jest+E2E) | 打开会话后标记为已读 | API 返回 success，未读计数清零 | [x] |
| 6.3 | [MANUAL] | 已读状态同步到发送方 | 发送方看到消息已被对方阅读 | [ ] |

## 7. 自动化测试汇总

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 7.1 | [AUTO] | `pnpm test` 全部通过 | Server 32 + Client 23 tests 全部通过 | [x] |
| 7.2 | [MANUAL] | WebSocket 连接认证测试 | 有效/无效 Session 场景覆盖（待补充专项测试） | [ ] |
| 7.3 | [AUTO] (Jest+E2E) | 消息收发测试 | Chat API 10 个用例 + E2E 10 个用例（含接收方会话列表+在线状态） | [x] |
| 7.4 | [AUTO] (RTL) | 前端聊天组件渲染测试 | ConversationList 4 用例 + ChatWindow 4 用例 | [x] |
