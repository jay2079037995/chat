# v0.6.0 - 聊天记录与搜索 测试文档

> 标记说明: `[AUTO]` = 自动化测试覆盖（Jest / E2E）| `[MANUAL]` = 需人工验证

## 1. 历史消息加载

### 1.1 后端 API

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 1.1.1 | [AUTO] | GET `/api/chat/conversations/:id/messages` 首页 | 返回最新的 N 条消息（默认 50 条） | [x] |
| 1.1.2 | [AUTO] | 带 offset 参数分页查询 | 返回指定偏移量后的消息 | [x] |
| 1.1.3 | [AUTO] | offset 超出范围 | 返回空数组 | [x] |
| 1.1.4 | [AUTO] | 查询不属于自己的会话 | 返回 403 | [x] |
| 1.1.5 | [AUTO] | 会话不存在 | 返回 404 | [x] |

### 1.2 前端交互

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 1.2.1 | [MANUAL] | 进入聊天窗口加载最新消息 | 显示最近的消息，滚动到底部 | [ ] |
| 1.2.2 | [MANUAL] | 上滑加载更多历史消息 | 触顶时自动加载上一页消息 | [ ] |
| 1.2.3 | [MANUAL] | 加载中显示 loading 指示 | 上滑加载时显示 Spin 动画 | [ ] |
| 1.2.4 | [MANUAL] | 所有历史消息加载完毕 | 显示"没有更多消息"提示 | [ ] |
| 1.2.5 | [MANUAL] | 加载后保持滚动位置 | 新消息插入顶部后不影响当前阅读位置 | [ ] |

## 2. 聊天记录搜索

### 2.1 后端 API

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 2.1.1 | [AUTO] | GET `/api/chat/messages/search?q=keyword` 有结果 | 返回包含关键词的消息列表 | [x] |
| 2.1.2 | [AUTO] | 搜索无结果 | 返回空数组 | [x] |
| 2.1.3 | [AUTO] | 搜索关键词为空 | 返回 400 | [x] |
| 2.1.4 | [AUTO] | 搜索关键词缺失 | 返回 400 | [x] |
| 2.1.5 | [AUTO] | 仅搜索自己参与的会话 | 不会返回无权限的会话消息 | [x] |

### 2.2 前端交互

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 2.2.1 | [AUTO] | 搜索弹窗渲染 | 显示标题 + 搜索输入框 | [x] |
| 2.2.2 | [AUTO] | 输入关键词搜索 | 显示匹配的消息列表 | [x] |
| 2.2.3 | [AUTO] | 搜索无结果 | 显示"未找到相关消息"提示 | [x] |
| 2.2.4 | [AUTO] | 不可见时不渲染 | visible=false 时无弹窗 | [x] |
| 2.2.5 | [MANUAL] | 搜索结果高亮关键词 | 匹配的文字 `<mark>` 高亮显示 | [ ] |
| 2.2.6 | [MANUAL] | 点击搜索结果跳转会话 | 跳转到对应会话 | [ ] |
| 2.2.7 | [MANUAL] | Home 页搜索按钮 | 点击「搜索」打开搜索弹窗 | [ ] |

## 3. E2E 测试

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 3.1 | [AUTO] | 搜索消息返回匹配结果 (API) | `GET /api/chat/messages/search?q=xxx` 返回匹配消息 | [x] |
| 3.2 | [AUTO] | 搜索无结果返回空 (API) | 返回空数组 | [x] |
| 3.3 | [AUTO] | 历史消息分页正确 (API) | offset/limit 分页 + 超出范围空数组 | [x] |

## 4. 自动化测试汇总

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 4.1 | [AUTO] | 后端 search.test.ts 通过 | jest --runInBand 全部绿色（10 tests） | [x] |
| 4.2 | [AUTO] | 前端 MessageSearch.test.tsx 通过 | jest 全部绿色（4 tests） | [x] |
| 4.3 | [AUTO] | ChatWindow 测试兼容（原测试不 break） | 添加 hasMore/loadingMore mock 后通过 | [x] |
| 4.4 | [AUTO] | E2E history-search.spec.ts 通过 | Playwright 全部绿色（3 tests） | [x] |
| 4.5 | [AUTO] | scripts/test.sh v0.6.0 结构检查通过 | MessageSearch 组件文件存在 | [x] |
