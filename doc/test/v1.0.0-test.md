# v1.0.0 - @提及功能测试文档

> 标记说明: `[AUTO]` = 自动化测试覆盖 | `[MANUAL]` = 需人工验证

## 1. 共享类型

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 1.1 | [AUTO] | Message.mentions 字段 | Message 接口包含 `mentions?: string[]` | [x] |
| 1.2 | [AUTO] | mention:notify 事件 | ServerToClientEvents 包含 `mention:notify` 事件定义 | [x] |

## 2. 服务端 — @解析

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 2.1 | [AUTO] | 单个 @username 解析 | 发送 `@alice` → message.mentions 包含 alice 的 userId | [x] |
| 2.2 | [AUTO] | 多个 @username 解析 | 发送 `@alice @bob` → mentions 包含两个 userId | [x] |
| 2.3 | [AUTO] | 不存在的用户 @提及 | 发送 `@ghost` → mentions 为空，消息正常保存 | [x] |
| 2.4 | [AUTO] | 非文本类型不解析 | type=image 的消息不做 @解析 | [x] |
| 2.5 | [AUTO] | Markdown 类型支持解析 | type=markdown 的 @username 正常解析 | [x] |
| 2.6 | [AUTO] | 重复 @去重 | 发送 `@alice @alice` → mentions 仅包含一个 userId | [x] |
| 2.7 | [AUTO] | Redis 序列化/反序列化 | mentions 字段正确序列化为 JSON 字符串并反序列化 | [x] |

## 3. 服务端 — 通知

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 3.1 | [MANUAL] | mention:notify 事件触发 | 消息含 mentions 时，向被 @ 用户 emit mention:notify | [x] |
| 3.2 | [MANUAL] | 发送者不自通知 | 发送者 @ 自己时不发送 mention:notify | [x] |

## 4. 前端 — MentionInput 组件

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 4.1 | [AUTO] | 组件渲染 | MentionInput 渲染 textbox 和 placeholder | [x] |
| 4.2 | [AUTO] | 显示输入值 | 传入 value 正确显示在输入框中 | [x] |
| 4.3 | [AUTO] | 空成员列表 | members 为空时正常渲染不报错 | [x] |
| 4.4 | [MANUAL] | @ 触发下拉 | 群聊输入 @ 弹出成员下拉列表 | [x] |
| 4.5 | [MANUAL] | 选择成员插入 | 下拉列表选择成员 → 插入 @username | [x] |
| 4.6 | [MANUAL] | 过滤匹配 | 输入 @al → 列表过滤显示 alice | [x] |

## 5. 前端 — ChatWindow 集成

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 5.1 | [MANUAL] | 群聊使用 MentionInput | 群聊时输入区域使用 MentionInput 组件 | [x] |
| 5.2 | [MANUAL] | 私聊保持 TextArea | 私聊时输入区域仍使用普通 TextArea | [x] |
| 5.3 | [AUTO] | participantNames 传递 | MessageBubble 接收 participantNames prop | [x] |

## 6. 前端 — @高亮显示

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 6.1 | [MANUAL] | 对方气泡中 @ 高亮 | @username 使用主题色背景高亮 | [x] |
| 6.2 | [MANUAL] | 自己气泡中 @ 高亮 | @username 使用白色半透明背景高亮 | [x] |
| 6.3 | [MANUAL] | 非 @ 文本正常显示 | 不含 @ 的文本不受影响 | [x] |

## 7. 前端 — 通知

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 7.1 | [MANUAL] | 收到 @通知 | 被 @ 时右上角弹出 notification 提示 | [x] |
| 7.2 | [MANUAL] | 通知内容正确 | 显示 "xxx @了你" + 消息内容预览 | [x] |
| 7.3 | [MANUAL] | 通知自动消失 | 5 秒后自动关闭 | [x] |

## 8. 构建与测试

| # | 类型 | 测试项 | 预期结果 | 状态 |
|---|------|--------|----------|------|
| 8.1 | [AUTO] | pnpm build | 4 个包全部编译成功 | [x] |
| 8.2 | [AUTO] | 服务端测试 | 80 tests 全部通过（含 7 个 mention 测试） | [x] |
| 8.3 | [AUTO] | 客户端测试 | 71 tests 全部通过（含 3 个 MentionInput 测试） | [x] |
