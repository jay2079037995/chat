# v1.25.0 测试文档 — 本地 Agent (agent-app) 详细日志 + 实时步骤进度

## 自动化测试

### Server 测试
- [AUTO] `bot-step-progress-api.test.ts` — stepProgress/generationLog 端点逻辑（token 验证、botId 覆盖、数据保存）

### Agent-app 测试
- [AUTO] `botClient.test.ts` — reportStepProgress + saveGenerationLog 方法（方法存在性 + 网络错误处理）
- [AUTO] `agentManager.test.ts` — Agent 管理器（generateLegacy mock 兼容）

### 覆盖项
- [AUTO] stepProgress 端点 token 验证 → `bot-step-progress-api.test.ts` getBotIdByToken 测试
- [AUTO] generationLog 保存时 botId 强制覆盖 → `bot-step-progress-api.test.ts` 保存验证
- [AUTO] 空 steps 错误日志也能保存 → `bot-step-progress-api.test.ts` 空 steps 测试
- [AUTO] BotClient.reportStepProgress 方法可用 → `botClient.test.ts` 方法存在性 + 网络错误
- [AUTO] BotClient.saveGenerationLog 方法可用 → `botClient.test.ts` 方法存在性 + 网络错误
- [AUTO] agent-app Bot 对话显示步骤进度 → 复用 v1.24.0 `BotStepProgress.test.tsx` 已有覆盖
- [AUTO] BotLogViewer 显示 agent-app 日志 → 复用 v1.24.0 `BotLogViewer.test.tsx` 已有覆盖

## 构建验证
- [AUTO] `pnpm build` 全部编译成功
- [AUTO] `pnpm test` 全部通过（559 tests）
