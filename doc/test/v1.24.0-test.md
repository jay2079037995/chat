# v1.24.0 测试文档 — Agent 详细运行日志 + 实时步骤进度显示

## 自动化测试

### Server 测试
- [AUTO] `bot-service-logs.test.ts` — AgentGenerationLog CRUD（save/get/clear/delete cleanup）
- [AUTO] `server-tool-bridge.test.ts` — onStepProgress/onToolLog 回调触发验证（start/complete/error、bash_exec/read_file/send_file_to_chat/present_choices、命令截断）

### Client 测试
- [AUTO] `BotStepIndicator.test.tsx` — 步骤指示器渲染、各步骤类型、计时器、截断
- [AUTO] `BotLogViewer.test.tsx` — Agent 日志 Tab + LLM 日志 Tab 切换 + 步骤时间线 + 展开/刷新/清空
- [AUTO] `BotStepProgress.test.tsx` — Store setBotStepProgress 状态管理 + BotStepIndicator 集成渲染 + socket bot:step-progress 处理逻辑（延迟清除、timestamp 防覆盖）

### 覆盖项（原手动 → 自动）
- [AUTO] 步骤指示器显示"正在生成回复" → `BotStepProgress.test.tsx` generating 步骤渲染
- [AUTO] bash_exec 显示"正在执行命令"（命令内容） → `BotStepProgress.test.tsx` bash_exec 步骤 + detail
- [AUTO] read_file 显示"正在读取文件"（文件路径） → `BotStepProgress.test.tsx` read_file 步骤 + detail
- [AUTO] 工具完成 1.5 秒自动消失 → `BotStepProgress.test.tsx` complete 延迟清除
- [AUTO] 错误状态显示 → `BotStepProgress.test.tsx` error 状态不崩溃
- [AUTO] BotLogViewer 默认显示 Agent 日志 Tab → `BotLogViewer.test.tsx` Segmented 默认值
- [AUTO] Agent 日志列表（时间/耗时/步骤数/状态） → `BotLogViewer.test.tsx` agent log 渲染
- [AUTO] 展开批次 → 时间线显示步骤 → `BotLogViewer.test.tsx` 展开时间线
- [AUTO] 展开步骤 → input/output JSON → `BotLogViewer.test.tsx` 展开步骤 JSON
- [AUTO] 切换到 LLM 日志 Tab → `BotLogViewer.test.tsx` Tab 切换
- [AUTO] 清空日志 → `BotLogViewer.test.tsx` 清空交互
- [AUTO] 刷新按钮 → `BotLogViewer.test.tsx` 刷新交互
- [AUTO] generation-logs API 分页/清空 → `bot-service-logs.test.ts` getAgentGenerationLogs/clearAgentGenerationLogs
- [AUTO] ServerToolBridge 回调触发 → `server-tool-bridge.test.ts` dispatchTool/send_file_to_chat/present_choices

## 构建验证
- [AUTO] `pnpm build` 全部编译成功
- [AUTO] `pnpm test` 全部通过（549 tests）
