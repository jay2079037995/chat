# v0.2.0 - 用户系统 测试文档

## 1. 注册功能

### 1.1 后端 API

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 1.1.1 | POST `/api/auth/register` 正常注册 | 返回 200，body 包含 token + sessionId + user 信息 | [ ] |
| 1.1.2 | 注册时用户名为空 | 返回 400，提示用户名不能为空 | [ ] |
| 1.1.3 | 注册时密码为空 | 返回 400，提示密码不能为空 | [ ] |
| 1.1.4 | 注册时用户名已存在 | 返回 409，提示用户名已被占用 | [ ] |
| 1.1.5 | 密码存储为 bcrypt 加密 | Redis 中存储的密码不是明文 | [ ] |

### 1.2 前端页面

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 1.2.1 | 访问注册页面 | 显示用户名、密码输入框和注册按钮 | [ ] |
| 1.2.2 | 用户名为空提交 | 表单校验提示"请输入用户名" | [ ] |
| 1.2.3 | 密码为空提交 | 表单校验提示"请输入密码" | [ ] |
| 1.2.4 | 正常注册 | 注册成功，自动登录并跳转到主页 | [ ] |
| 1.2.5 | 用户名重复注册 | 显示错误提示"用户名已被占用" | [ ] |

## 2. 登录功能

### 2.1 后端 API

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 2.1.1 | POST `/api/auth/login` 正确凭据 | 返回 200，body 包含 token + sessionId + user | [ ] |
| 2.1.2 | 登录时用户名不存在 | 返回 401，提示用户名或密码错误 | [ ] |
| 2.1.3 | 登录时密码错误 | 返回 401，提示用户名或密码错误 | [ ] |
| 2.1.4 | 登录成功后 Redis 中存在 Session | 能通过 Session ID 查询到用户信息 | [ ] |

### 2.2 前端页面

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 2.2.1 | 访问登录页面 | 显示用户名、密码输入框和登录按钮 | [ ] |
| 2.2.2 | 正常登录 | 登录成功，Token 存入 localStorage，跳转主页 | [ ] |
| 2.2.3 | 用户名或密码错误 | 显示错误提示 | [ ] |
| 2.2.4 | 登录页有"去注册"链接 | 点击可跳转到注册页 | [ ] |

## 3. 自动登录（Token + Session）

### 3.1 后端 API

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 3.1.1 | POST `/api/auth/session` 有效 Token | 返回 200，body 包含新的 sessionId + user | [ ] |
| 3.1.2 | POST `/api/auth/session` 无效 Token | 返回 401，提示 Token 无效 | [ ] |
| 3.1.3 | POST `/api/auth/session` 过期 Token | 返回 401，提示 Token 已过期 | [ ] |

### 3.2 前端逻辑

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 3.2.1 | localStorage 有有效 Token 时刷新页面 | 自动登录成功，获取新 Session，进入主页 | [ ] |
| 3.2.2 | localStorage 有无效 Token 时刷新页面 | 自动登录失败，清除 Token，跳转登录页 | [ ] |
| 3.2.3 | localStorage 无 Token 时访问主页 | 路由守卫拦截，跳转登录页 | [ ] |

## 4. 登出功能

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 4.1 | POST `/api/auth/logout` 有效 Session | 返回 200，Redis 中 Session 被删除 | [ ] |
| 4.2 | 前端点击登出 | 清除 localStorage Token，清除内存 Session，跳转登录页 | [ ] |
| 4.3 | 登出后用原 Session 请求 API | 返回 401，Session 无效 | [ ] |

## 5. Session 验证

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 5.1 | GET `/api/auth/me` 携带有效 Session | 返回 200，返回当前用户信息 | [ ] |
| 5.2 | GET `/api/auth/me` 无 Session | 返回 401 | [ ] |
| 5.3 | GET `/api/auth/me` 无效 Session | 返回 401 | [ ] |

## 6. 用户搜索

### 6.1 后端 API

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 6.1.1 | GET `/api/users/search?q=test` 有匹配用户 | 返回匹配的用户列表（不含密码） | [ ] |
| 6.1.2 | GET `/api/users/search?q=xxx` 无匹配用户 | 返回空数组 | [ ] |
| 6.1.3 | 搜索结果不包含自己 | 当前登录用户不出现在搜索结果中 | [ ] |

### 6.2 前端组件

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 6.2.1 | 输入关键词搜索 | 显示匹配的用户列表 | [ ] |
| 6.2.2 | 搜索无结果 | 显示"未找到用户"提示 | [ ] |

## 7. 自动化测试

| # | 测试项 | 预期结果 | 状态 |
|---|--------|----------|------|
| 7.1 | `pnpm test` 全部通过 | 所有 test suites 通过 | [ ] |
| 7.2 | 后端注册 API 测试 | 覆盖正常注册 + 重复用户名 + 参数校验 | [ ] |
| 7.3 | 后端登录 API 测试 | 覆盖正常登录 + 错误凭据 | [ ] |
| 7.4 | 后端 Token 自动登录测试 | 覆盖有效/无效 Token | [ ] |
| 7.5 | 后端 Session 验证测试 | 覆盖有效/无效 Session | [ ] |
| 7.6 | 后端搜索 API 测试 | 覆盖有结果 + 无结果 | [ ] |
| 7.7 | 前端注册/登录组件测试 | 表单渲染 + 交互测试 | [ ] |
