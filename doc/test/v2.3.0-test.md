# v2.3.0 测试文档 — 多模型工具 + 自我赋能

## 自动化测试

### Server 测试
- [AUTO] `server-tool-bridge.test.ts` — list_models 工具返回 MODEL_PROVIDERS 中所有模型列表
- [AUTO] `server-tool-bridge.test.ts` — switch_model 工具通过 onSwitchModel 回调更新 currentModelStr
- [AUTO] `server-tool-bridge.test.ts` — list_models/switch_model 为本地工具（不走 ToolDispatcher）
- [AUTO] `bot-llm-integration.test.ts` — /chat 端点动态模型引用验证

### 覆盖项
- [AUTO] list_models/switch_model 工具定义 → TypeScript 编译验证
- [AUTO] currentModelStr 动态引用 → /chat 端点逻辑
- [AUTO] 自我赋能系统提示（决策树）→ systemPrompt 字符串包含验证
- [AUTO] switch_model 下轮生效机制 → Agent maxSteps 循环逻辑

## 构建验证
- [AUTO] `pnpm build` 全部编译成功
- [AUTO] `pnpm test` 全部通过
